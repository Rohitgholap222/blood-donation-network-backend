spring.application.name=smart-blood-network
spring.config.import=optional:file:.env[.properties]
server.port=${PORT:8080}

# Database Config
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://aws-1-ap-northeast-2.pooler.supabase.com:6543/postgres}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres.fhrndqmfmmmhueioltgt}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:blood_network_rohit172}
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.jpa.show-sql=${SPRING_JPA_SHOW_SQL:false}
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT Config
jwt.secret=${JWT_SECRET:smartBloodNetworkJwtSecretKey2026!!}
jwt.expiration=${JWT_EXPIRATION:86400000}
jwt.refreshExpiration=${JWT_REFRESH_EXPIRATION:604800000}

# Redis Config
spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}
spring.data.redis.password=${REDIS_PASSWORD:}

# Admin Seed Config
app.admin.seed.enabled=${ADMIN_SEED_ENABLED:false}
app.admin.email=${ADMIN_EMAIL:admin@bloodnetwork.com}
app.admin.password=${ADMIN_PASSWORD:}
app.admin.name=${ADMIN_NAME:System Admin}
app.admin.phone=${ADMIN_PHONE:0000000000}

# Logging
logging.level.org.springframework.web=INFO
logging.level.com.bloodnetwork=${APP_LOG_LEVEL:INFO}

# Frontend Config
app.frontend.url=${FRONTEND_URL:http://localhost:3000}
